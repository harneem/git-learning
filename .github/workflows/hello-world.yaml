name: Print repo name

on:
  push:
    branches:
      - master  

jobs:
  Print-using-context:
    runs-on: [self-hosted,linux]
   
    steps:
      - name: Print repo name
        run: echo "I am currently using repository ${{github.repository }}"
        