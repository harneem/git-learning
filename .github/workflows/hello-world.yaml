name: Print branch name

on:
  push:
    branches:
      - master  

jobs:
  Print-using-context:
    runs-on: [self-hosted,linux]
   
    steps:
      - name: Print branch name
        run: echo "I am currently on branch ${{github.ref_name }}"
        