name: Print Repo name using github context

on:
  push:
    branches:
      - master  

jobs:
  Print-using-context:
    runs-on: [self-hosted,linux]
    if : "${{github.ref_name==master}}"
    steps:
      - name: Print message when branch is main
        run: echo "Hello we are on master branch!"
        