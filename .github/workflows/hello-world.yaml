name: Print Repo name using github context

on:
  push:
    branches:
      - master  

jobs:
  Print-reponame-using-context:
    runs-on: [self-hosted,linux]
    steps:
      - name: Print branch name
        run: echo "Triggered on repo ${{github.repository}}"
        