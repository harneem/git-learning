name: Matrix Build
on: push

jobs:
  build:
    runs-on: [self-hosted,linux]
    strategy:
      matrix:
        node: [14, 16, 18]
    steps:
      - name: Setup Node
        uses: actions/setup-node@v3