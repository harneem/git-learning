name: Print name of github user using github context

on:
  push:
    branches:
      - master  

jobs:
  Print-branchname-using-context:
    runs-on: [self-hosted,linux]
    steps:
      - name: Print branch name
        run: echo "Triggered on branch ${{github.ref_name}}"
        